<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abhishek.workplace">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".weather.WeatherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ThreeTenAbp.ThreeTenAbpActivity">

        </activity>
        <activity android:name=".EasyReveal.TwoActivity" />
        <activity android:name=".EasyReveal.OneActivity" />
        <activity android:name=".TextToImage.TextToImageActivity"></activity>
        <activity android:name=".location.LocationActivity" />
        <activity android:name=".mvvm.login.MvvLoginActivity" />
        <activity android:name=".mvp.addPost.AddPostActivity" />
        <activity android:name=".mvp.home.HomeMvpActivity" />
        <activity android:name=".mvp.login.MvpLoginActivity" />
        <activity android:name=".OneAVibraterActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>